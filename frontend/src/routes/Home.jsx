import { Outlet } from "react-router-dom";

const Home = () => {
    console.log("Home component rendered");
    return (
        <div>
            <h1>Home Page</h1>
        </div>
    );
};

export default Home;
